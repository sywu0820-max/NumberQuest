# 10+n Composer microtoy v0

Candidate A branches directly from frozen PR #20 head `140d492c76b7c60c635e594865a7f74f53479855`. It is hidden at `ten-plus-n.html?prototype=ten-plus-n-v0&n=4&founder=1` and has no ordinary Number Quest entry.

The surface shows one intact reversible ten plus `n` stable loose objects for `0 <= n <= 9`. Only while the ten is intact may the compact numeral `10+n` appear. Opening and re-closing moves the same ten DOM nodes; every extra node remains unchanged. For `n=0`, no placeholder extra appears.

Founder path: open the default, tap the ten to open it, re-close it with taps or a drag, then compare `n=0` and `n=9`. Judge whether 10–19 reads as one ten plus loose ones without recounting the whole set.
